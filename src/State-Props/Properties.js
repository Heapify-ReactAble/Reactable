import React from "react";

const Properties = (props) => {
  return (
    <div>
      <h1 style={{ backgroundColor: props.textColor }}>Heading</h1>
    </div>
  );
};

export default Properties;
// state
