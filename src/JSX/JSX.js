import React from "react";

const JSX = () => {
  const value = 11;

  return <div>{value <= 10 ? "heapify" : "Hello"}</div>;
};

export default JSX;


// Babel - transcompiler
// React.createElement();